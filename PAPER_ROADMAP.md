# 小论文端到端执行路线图

## 0. 项目目标与完成定义

目标论文：面向未见拓扑泛化的配电网物理引导图扩散潮流数据生成。

论文只有在以下证据全部具备后才算完成：

- 可复现的数据生成配置和数据质量报告；
- 可训练的 Vector-DDPM、TopoGraph-DDPM 和 TopoPF-Diff；
- 拓扑相关交流潮流残差经过单元测试与 pandapower 交叉验证；
- 至少 3 个随机种子的主实验结果；
- 统计保真度、物理一致性、多样性和未见拓扑泛化指标；
- 物理约束、图结构和边特征的消融实验；
- 论文中的每个数字均可追溯到实验结果文件；
- 全文、图表、公式、引用与复现命令通过投稿前检查。

## 1. 当前基线（2026-08-24）

### 已完成

- IEEE33 Baran-Wu 数据构建器；
- 37 条候选支路、每个辐射拓扑 32 条活动支路；
- 主图、活动边索引、拓扑掩码和边特征表示；
- 300 个拓扑 × 500 个工况的服务器数据集；
- 按拓扑划分训练/验证/测试集；
- 拓扑条件图去噪器与标准噪声预测 DDPM；
- 小规模训练和采样冒烟检查。

### 尚未完成

- 拓扑相关导纳矩阵和交流潮流残差；
- 物理损失和电压边界损失；
- Vector-DDPM 与 Wang 风格物理扩散基线；
- 统一生成与评价程序；
- 正式训练、消融、压力测试；
- 实验图表及论文正文。

## 2. 代码与材料的目标结构

```text
data_iEEE33/
├── configs/                  # 正式实验配置，每次实验不可手工改名覆盖
├── data/                     # 本地小数据；正式大数据不提交 Git
├── docs/                     # 数据字典、公式说明、实验记录
├── outputs/
│   └── <experiment_id>/
│       ├── config.json       # 实际运行参数
│       ├── history.json      # 每轮训练记录
│       ├── best.pt
│       ├── generated.npz
│       ├── metrics.json
│       └── run.log
├── results/
│   ├── raw/                  # 各随机种子的原始指标
│   ├── tables/               # 聚合后的论文表格
│   └── figures/              # 论文图
├── src/ieee33_diffusion/
│   ├── build_dataset.py
│   ├── model.py
│   ├── physics.py            # 导纳矩阵、AC残差、边界损失
│   ├── train_infer.py
│   ├── evaluate.py           # 统一指标
│   └── aggregate_results.py  # 多种子聚合
└── tests/
    ├── test_topology.py
    ├── test_physics.py
    └── test_model.py
```

## 3. 阶段一：锁定研究协议与数据证据

### 任务

1. 保存正式数据集的 SHA256、文件大小、生成命令和构建日志；
2. 运行数据审计：样本数、拓扑数、每拓扑边数、划分数量、重复拓扑、各通道范围、非有限值、质量标记；
3. 固定训练集归一化统计，只允许使用训练拓扑计算均值与标准差；
4. 固定测试协议：测试拓扑不得参与模型选择或超参数调整；
5. 生成压力数据集，或至少保留一个比训练区间更宽的评估集。

### 验收门槛

- 数据审计程序返回通过；
- 210/30/60 个拓扑分别属于训练/验证/测试（300 拓扑、70/10/20 时）；
- 不存在拓扑泄漏和 NaN/Inf；
- 每个拓扑恰有 32 条活动边且为连通树；
- 数据哈希和元数据写入 `docs/dataset_manifest.md`。

## 4. 阶段二：实现拓扑相关物理模块

### 4.1 导纳矩阵

根据每个样本的活动边及支路参数构造复数节点导纳矩阵 \(Y(G)=G+jB\)。实现时先支持当前平衡单相、无变压器的 IEEE33 场景，并把假设写入文档。

### 4.2 功率残差

对预测清洁状态 \(\hat{x}_0=[P,Q,V,\theta]\) 计算：

\[
\Delta P_i=P_i-V_i\sum_jV_j(G_{ij}\cos\theta_{ij}+B_{ij}\sin\theta_{ij}),
\]

\[
\Delta Q_i=Q_i-V_i\sum_jV_j(G_{ij}\sin\theta_{ij}-B_{ij}\cos\theta_{ij}).
\]

需要统一注入正负号、MW/Mvar 与 p.u. 基值。Slack 节点是否参与损失要做成明确配置，并通过消融确定。

### 4.3 联合损失

首版只实现必要组件：

\[
L=L_{noise}+\lambda_{pf}w(t)L_{pf}+\lambda_{v}L_v.
\]

不要在首版同时加入潮流投影、可学习噪声日程和复杂分类器引导，以免无法识别性能来源。

### 验收门槛

- 使用数据集中的真实潮流样本，PyTorch 残差与 pandapower 平衡量在预设容差内一致；
- 人为扰动电压或相角后，残差明显增大；
- 物理损失可反向传播且梯度有限；
- 批量拓扑输入的形状、设备和精度均正确；
- 小数据集上联合训练无 NaN，且各损失量级被记录。

## 5. 阶段三：建立模型对照组

### 必做模型

| 代号 | 模型 | 图消息传递 | 拓扑条件 | 物理损失 | 证明内容 |
|---|---|---:|---:|---:|---|
| B0 | Vector-DDPM | 否 | 是 | 否 | 非图条件扩散基线 |
| B1 | PI-Vector-DDPM | 否 | 是 | 是 | 对齐 Wang 论文的物理思路 |
| B2 | TopoGraph-DDPM | 是 | 是 | 否 | 图结构贡献 |
| M | TopoPF-Diff | 是 | 是 | 是 | 完整方法 |

### 公平性规则

- 使用相同训练/验证/测试拓扑；
- 使用相同通道归一化、扩散步数、训练轮数和早停规则；
- 尽可能使参数量处于同一数量级；
- 所有模型使用种子 2026、2027、2028；
- 超参数只能依据验证拓扑选择；
- 测试集只在模型和超参数锁定后统一运行。

### 验收门槛

- 四组模型均可用同一命令接口训练、采样；
- 每次运行自动保存完整配置、Git commit、种子和环境信息；
- `best.pt` 的选择标准固定且不使用测试指标。

## 6. 阶段四：统一评价流水线

### 6.1 统计保真度

- 每通道 Wasserstein-1 距离；
- MMD；
- 节点均值与标准差误差；
- 节点相关矩阵 Frobenius 误差；
- 代表节点及全网聚合分布图。

### 6.2 物理一致性

- \(|\Delta P|+|\Delta Q|\) 的均值、中位数、95% 和 99% 分位；
- 满足预设残差阈值的物理可行率；
- 电压越限率；
- 通过 pandapower 潮流回代或独立残差计算交叉检查。

### 6.3 多样性与记忆检查

- 生成/真实标准差之比；
- 最近训练样本距离；
- 覆盖率指标；
- 生成样本之间的成对距离。

### 6.4 未见拓扑泛化

- 已见验证拓扑与未见测试拓扑分开报告；
- 按相对基准拓扑的开关距离分组；
- 每个测试拓扑生成相同数量样本，避免样本数量加权偏差。

### 验收门槛

- 指标函数有合成数据单元测试；
- 同一个 `generated.npz` 重复评价得到相同结果；
- 总体指标可追溯到逐拓扑原始指标；
- 所有图和表由脚本生成，不手工抄数。

## 7. 阶段五：正式实验顺序

不要一次提交全部大实验。按下列门控顺序执行：

### E0：数据与物理残差验证

目的：证明数据和公式正确。失败时不得进入正式训练。

### E1：单拓扑/小数据过拟合

目的：确认模型、归一化、反归一化及采样链路能工作。

### E2：12—30 个拓扑的开发实验

目的：选择 \(\lambda_{pf}\)、时间权重和基本训练配置。只使用训练与验证拓扑。

### E3：完整数据单种子预实验

目的：估计显存、训练时间、收敛轮数和生成成本，发现流水线问题。

### E4：四模型 × 三种子的主实验

目的：形成论文表2的总体结果。

### E5：未见拓扑分层实验

目的：形成泛化图和按拓扑距离分组结果。

### E6：消融实验

最低组合：无物理损失、无边参数、忽略活动边、不同 \(\lambda_{pf}\)、不同时间权重。

### E7：压力与稳健性实验

使用更宽负荷区间或电压越限数据，明确模型最适用与最不适用的区域。

## 8. 实验命名与记录规范

实验 ID 统一为：

```text
<model>_<dataset>_seed<seed>_<date>
```

例如：

```text
topopf_feasibleT300_seed2026_20260824
```

每个实验目录至少保存：

- 实际配置；
- Git commit；
- Python、PyTorch、CUDA 和 GPU 信息；
- 训练/验证损失历史；
- 最优与最后一个检查点；
- 生成样本；
- 逐拓扑指标；
- 完整标准输出和错误日志。

禁止覆盖已有实验目录；参数变化必须产生新的实验 ID。

## 9. 论文写作工作流

### 9.1 实验前可写

- 研究问题和符号表；
- 变拓扑潮流问题建模；
- 数据与拓扑表示；
- 图条件扩散网络；
- 数据构建与拓扑级划分。

### 9.2 实验中同步写

- 物理损失最终公式与实现假设；
- 实验环境、超参数和基线；
- 图1方法框架、图2拓扑表示；
- 相关工作对比矩阵。

### 9.3 实验后再写

- 摘要中的结果数字；
- 引言贡献和结果预览；
- 结果分析、机制解释和局限；
- 结论。

### 9.4 每个结果段落的固定结构

1. 先写本实验回答的问题；
2. 再给表/图和定量结果；
3. 比较基线并解释可能机制；
4. 指出例外或失败区域；
5. 用一句 Takeaway 回扣论文贡献。

## 10. 建议时间安排

下面按六周安排；若投稿期限更近，可压缩日程但不能跳过验收门槛。

| 周次 | 主要任务 | 周末交付物 |
|---|---|---|
| 第1周 | 数据审计、物理残差、单元测试 | `physics.py`、测试报告、数据清单 |
| 第2周 | 联合损失、四类模型接口、小数据调试 | 可重复的小规模训练结果 |
| 第3周 | 评价脚本、单种子完整预实验 | 指标文件、资源与时长估计 |
| 第4周 | 三种子主实验、未见拓扑实验 | 主结果表初稿 |
| 第5周 | 消融、压力测试、论文图表 | 完整图表与结果结论 |
| 第6周 | 全文写作、引用核验、复现和投稿检查 | 可投稿稿件与复现包 |

## 11. 每次协作时的汇报模板

用户从服务器返回以下内容即可继续排查和决策：

```text
实验ID：
Git commit：
执行命令：
GPU型号/显存：
运行状态：完成/失败/仍在运行
关键日志最后50行：
输出文件列表：
```

后续每一轮协作都按“当前证据 → 是否通过阶段门槛 → 下一条命令/修改 → 预期输出”推进。

## 12. 当前下一步

立即开始阶段一和阶段二：

1. 增加数据审计与正式数据清单；
2. 实现 `physics.py`；
3. 用真实数据样本和人为扰动样本验证 AC 残差；
4. 通过后再把物理损失接入训练。

在上述四项通过前，不启动服务器上的正式模型训练。
